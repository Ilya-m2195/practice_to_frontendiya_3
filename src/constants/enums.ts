export enum UsersKeys {
  phone = 'phone',
  nickname = 'nickname',
  fullName = 'fullName',
  role = 'role',
  email = 'email',
  id = 'id',
  balance = 'balance',
}

export enum Languages {
  en = 'en',
  ru = 'ru',
}

export enum NamesActiveStyles {
  activeStyle = 'active',
  itemStyle = 'item',
}

export enum ColorTheme {
  dark = 'dark',
  light = 'light',
}
